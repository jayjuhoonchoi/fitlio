apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: fitlio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15
        env:
        - name: POSTGRES_USER
          value: "fitlio"
        - name: POSTGRES_PASSWORD
          value: "fitlio123"
        - name: POSTGRES_DB
          value: "fitlio"
        ports:
        - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: fitlio
spec:
  selector:
    app: postgres
  ports:
  - port: 5432
    targetPort: 5432
